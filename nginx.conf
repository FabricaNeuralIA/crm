server {
    listen 80;
    server_name crm.fabricaneural.ia.br;

    root /usr/share/nginx/html;
    index login.html;

    location / {
        try_files $uri $uri/ $uri.html /login.html;
    }
}
